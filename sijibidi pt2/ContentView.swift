//
//  ContentView.swift
//  sijibidi pt2
//
//  Created by Jordan on 9/9/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("if u read dis u r sijibidi pt2")
                .font(/*@START_MENU_TOKEN@*/.title/*@END_MENU_TOKEN@*/)
                .bold()
            Text("poggies")
                
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
